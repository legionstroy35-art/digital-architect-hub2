/**
 * Общая глобальная конфигурация сайта для всех браузеров и посетителей.
 * Если вы хотите, чтобы плагины всегда загружались из вашей Google Таблицы
 * у любого посетителя сайта в любом браузере, вставьте ссылку на неё в defaultGoogleSheetUrl.
 */
export const SITE_CONFIG = {
  // Публичная ссылка на Google Таблицу (доступ: «Все, у кого есть ссылка — Читатель»)
  defaultGoogleSheetUrl: '',

  // Включить ли авто-подгрузку свежих данных из Google Таблицы при открытии сайта
  enableAutoFetchOnLoad: true,

  // Интервал периодического автообновления в фоне (в секундах). 60 сек = 1 минута.
  autoRefreshIntervalSeconds: 60,

  // Автоматически проверять обновления при переключении обратно на вкладку сайта
  refreshOnWindowFocus: true,
};
